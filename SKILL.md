---
name: ciwei-xingqiu
description: 刺猬星球skill. Write, diagnose, and revise production-ready AI image and video prompts using the current 刺猬星球 AIGC prompt lessons. Invoke when the user says 刺猬星球skill. Use for Seedance, 可灵, 即梦, 海螺, Sora, Runway, and similar visual generation tasks involving scripts, storyboards, references, identity consistency, blocking, camera, acting, motion, lighting, color, sound, long-video continuity, and Excel prompt delivery. Do not use for ordinary text-only prompting.
---

# 刺猬星球 AIGC 视觉提示词

当前版本：v1.0

把提示词当作可执行的导演指令。先确定素材职责和镜头目标，再写画面、动作与摄影机关系。不要用形容词堆砌代替设计。

## 工作方式

1. 判断任务属于文生图、图生视频/全能参考、首尾帧、文生视频、局部修改或长视频续写。
2. 读取用户提供的剧本、图片、视频和模型限制。已有素材优先于通用规则。
3. 为每份参考素材指定一个主要职责：人物与服装、场景、产品、构图、色彩、结构、动作或声音。
4. 缺少关键信息时，只询问会改变结果的问题；其余用明确假设继续。
5. 按 `references/video-prompt-guide.md` 选择模板并输出可直接复制的完整提示词。
6. 失败后按症状定位一个主要变量，只修改该变量；不要整段重写或继续无目的加词。

## 剧本交付约定

当用户提供剧本并明确要求使用本 Skill 时，默认完成剧本拆段、资产映射、镜头调度、连续性设计和生成验收规划，并将正式成果保存为 `.xlsx` 文件，而不是只在对话中给出散文式提示词。制作前读取 `references/excel-delivery.md`。

- 按自然表演、对白长度和动作完成度决定片段时长，不机械卡 15 秒。
- 一行对应一个可独立生成的视频片段；同一剧情需要多个镜头时，在该行完整提示词内按时间顺序写明。
- 提示词必须可直接复制；资产路径、上传顺序、参考职责、禁止用途和下一段衔接状态必须落入表格。
- 剧情视频的完整提示词必须同时完成三条导演轨：摄影机如何拍、演员如何演、光线与环境如何作用。不能只写镜头调度，也不能用“紧张、开心、自然表演”等抽象标签代替动作状态流。
- 演员轨写清人物当下目标、触发事件、注意力转移、视线、呼吸、重心、肩胯手脚联动、动作交接、情绪变化和收束；多人反应应有先后，不默认同步皱眉、转头或起跑。
- 容易被误读的动作先锁定基础姿态：站立/坐姿/跑动状态、躯干方向、双脚承重和道具初始位置。每个短节拍只保留一个主动作；动作链超出自然完成时间时延长片段或拆镜，不靠继续加词硬塞。
- 不同人物或物种的姿态分别成句，避免相邻的“压低重心、颈部前伸、缓慢接近”等词跨角色迁移。交付前通读连续词组形成的整体行为语义，确认不会把警觉、谨慎或准备误导成潜行、做贼、伏击或夸张表演。
- 光线环境轨写清物理光源、方向、软硬、色温、衰减、受光表面、前中后景、生活痕迹、风与材质反应，以及人物和道具在空间中的接触关系。
- 凡有对白，完整提示词必须逐字包含台词，并标明画外音或现场对白、开始时间、停顿、对象、距离、语气、口型与听者反应；不能只写“说完整原句”或把台词留在表格其他列。
- 如果用户只要求分析、诊断或口头建议，不强制创建 Excel。

## 输出要求

- 默认用中文，保留用户要求的专有名词、台词和屏幕文字。
- 有参考素材时，先给上传顺序和 `@图片1` 等职责映射，再给完整提示词。
- 视频提示词写清起始状态、触发、状态变化、摄影机路径和结束状态；需要时加入秒级时间轴。
- 剧情视频按需使用 `【演员表演与动作状态流】` 与 `【光线、环境与空间反应】` 独立段落；即使合并段落，这两类信息也不得缺失。
- 多人群戏写清每人的位置、朝向、移动权限、交互对象、动作交接顺序及位移结束时间；站位图仅作调度参考时，明确禁止进入成片。
- 只添加针对当前风险的负面约束。
- 首尾帧只用于必须精确锁定两个端点的转化；普通连续镜头优先全能参考或视频续写。
- 多镜头任务先建立人物、场景、产品、声音和色彩资产，再写逐镜头指令。
- 不把具体模型版本的旧结论当作永久规则；按当前模型能力和镜头目标选择。

## 交付硬门

交付前逐段检查，而不是只检查标题是否存在：

1. 完整提示词可单独复制，不依赖“同上”、其他列或聊天上下文。
2. 对白列中的每句台词在完整提示词中逐字出现，时长足以自然说完。
3. 摄影机、演员表演、光线环境三条导演轨都包含可执行关系。
4. 动作使用“触发 → 注意力/身体响应 → 主动作 → 次级反应 → 收束”，没有孤立动作清单。
5. Excel 标题与说明不挤在狭窄首列；上传引用、结束状态、衔接帧和验收项完整对应。
6. 高风险动作有明确基础姿态、可完成的动作预算和停止姿态；不同角色/物种的动作没有互相串用，连续措辞不会合成错误表演语义。

## 资料

- 写作、诊断和模板：`references/video-prompt-guide.md`
- 剧本类 Excel 交付：`references/excel-delivery.md`
- 已学习范围与下次更新方法：`references/progress.md`



